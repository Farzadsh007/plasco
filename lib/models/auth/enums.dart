enum role { admin, has_inspector, executive, not_access }

enum companyStatus {
  Pending,
  Rejected,
  Active,
  Deactive,
}

enum stakeHolderStatus {
  Pending,
  Active,
  Rejected,


}
enum MembershipStatus { Pending, Rejected, Active, Deactive }
enum actionStatus {
  not_complete,
  complete,
}
enum deadlineType { immediately, today, tomorrow, other }
enum anomalyStatus { closed, open, pending }
enum riskType { High, Low, Moderate, None }
